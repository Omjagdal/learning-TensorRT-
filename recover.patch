diff --git a/frontend/src/components/Header.jsx b/frontend/src/components/Header.jsx
index c56e0c39d..ca892ace4 100644
--- a/frontend/src/components/Header.jsx
+++ b/frontend/src/components/Header.jsx
@@ -10,10 +10,16 @@ export default function Header({ health, onClear, hasMessages, onShowKnowledge,
     <header className="flex items-center justify-between px-6 py-4 relative z-50" style={{ background: 'var(--bg-primary)' }}>
       {/* Brand */}
       <div className="flex items-center gap-3">
-        <div className="w-8 h-8 rounded-lg flex items-center justify-center overflow-hidden" style={{ background: 'var(--bg-card)', border: '1px solid var(--border)' }}>
-          <img src="/logo.png" alt="Logo" className="w-full h-full object-contain p-1" />
+        <div className="h-12 flex items-center justify-center overflow-hidden rounded bg-white">
+          <img src="/logo.png" alt="ISRA Vision" className="h-full object-contain p-1" />
+        </div>
+        <div className="flex flex-col justify-center">
+          <span className="text-[20px] font-bold tracking-tight uppercase leading-none mb-1">
+            <span style={{ color: 'var(--accent-primary)' }}>ISRA</span>{' '}
+            <span style={{ color: 'var(--text-primary)' }}>VISION</span>
+          </span>
+          <span className="text-[13px] font-semibold tracking-wider uppercase opacity-80" style={{ color: 'var(--text-secondary)' }}>OMI Assistant</span>
         </div>
-        <span className="text-[15px] font-semibold tracking-tight" style={{ color: 'var(--text-primary)' }}>ISRA Vision Chatbot Assistant</span>
       </div>
 
       {/* Right actions */}
@@ -21,30 +27,21 @@ export default function Header({ health, onClear, hasMessages, onShowKnowledge,
         {health && (
           <div className="hidden md:flex items-center gap-2">
             {health.offline_mode && (
-              <span className="tag text-emerald-400 border-emerald-900/50 bg-emerald-500/10">
+              <span className="tag text-red-400 border-red-900/50 bg-red-500/10">
                 <HardDrive size={11} /> Offline
               </span>
             )}
             <span className={clsx('tag', online
-              ? 'text-emerald-500 border-emerald-500/20 bg-emerald-500/10'
-              : 'text-red-400 border-red-500/20 bg-red-500/10'
+              ? 'text-sky-500 border-sky-500/20 bg-sky-500/10'
+              : 'text-gray-400 border-gray-500/20 bg-gray-500/10'
             )}>
-              <span className={clsx('w-1.5 h-1.5 rounded-full', online ? 'bg-emerald-400' : 'bg-red-400')} />
+              <span className={clsx('w-1.5 h-1.5 rounded-full', online ? 'bg-sky-500' : 'bg-gray-400')} />
               {online ? 'Connected' : 'Offline'}
             </span>
           </div>
         )}
 
-        <button
-          onClick={onShowKnowledge}
-          className="flex items-center gap-2 px-3.5 py-2 rounded-xl text-[13px] font-medium transition-all"
-          style={{ color: 'var(--text-secondary)' }}
-          onMouseEnter={e => e.currentTarget.style.background = 'var(--bg-tertiary)'}
-          onMouseLeave={e => e.currentTarget.style.background = 'transparent'}
-        >
-          <Database size={14} />
-          Knowledge
-        </button>
+
 
         {hasMessages && (
           <button
@@ -65,12 +62,6 @@ export default function Header({ health, onClear, hasMessages, onShowKnowledge,
           {isLight ? <Moon size={15} /> : <Sun size={15} />}
         </button>
 
-        {/* User avatar */}
-        <div className="w-8 h-8 rounded-full flex items-center justify-center text-[13px] font-semibold ml-1"
-          style={{ background: 'var(--bg-card)', border: '1px solid var(--border)', color: 'var(--text-secondary)' }}
-        >
-          M
-        </div>
       </div>
     </header>
   )
diff --git a/frontend/src/components/Message.jsx b/frontend/src/components/Message.jsx
index ed87b23db..bc025571d 100644
--- a/frontend/src/components/Message.jsx
+++ b/frontend/src/components/Message.jsx
@@ -123,7 +123,7 @@ function CodeBlock({ node, inline, className, children, ...props }) {
   }
 
   return (
-    <code className={clsx(className, 'text-emerald-400 px-1.5 py-0.5 rounded-md text-[14px] font-mono')}
+    <code className={clsx(className, 'text-sky-400 px-1.5 py-0.5 rounded-md text-[14px] font-mono')}
       style={{ background: 'var(--bg-tertiary)' }} {...props}>
       {children}
     </code>
@@ -155,7 +155,7 @@ export function TypingIndicator() {
     <div className="flex items-start gap-3 msg-enter">
       <div className="w-7 h-7 rounded-lg flex items-center justify-center shrink-0"
         style={{ background: 'var(--bg-card)', border: '1px solid var(--border)' }}>
-        <Sparkles size={14} className="text-emerald-400" />
+        <Sparkles size={14} className="text-sky-400" />
       </div>
       <div className="flex items-center gap-1.5 px-4 py-3 rounded-xl"
         style={{ background: 'var(--bg-card)', border: '1px solid var(--border)' }}>
@@ -174,7 +174,7 @@ const STAGE_CONFIG = {
   retrieve:  { icon: Search,     label: 'Retrieving',   color: 'text-blue-400' },
   rerank:    { icon: Search,     label: 'Reranking',    color: 'text-cyan-400' },
   generate:  { icon: Cpu,        label: 'Generating',   color: 'text-purple-400' },
-  validate:  { icon: Shield,     label: 'Validating',   color: 'text-emerald-400' },
+  validate:  { icon: Shield,     label: 'Validating',   color: 'text-sky-400' },
   fallback:  { icon: BookOpen,   label: 'Fallback',     color: 'text-orange-400' },
 }
 
@@ -237,7 +237,7 @@ function SourceItem({ source, index }) {
         )}
         <div className="flex gap-1 shrink-0">
           <span className={clsx('px-1.5 py-0.5 rounded-md text-[11px] font-mono font-medium',
-            score >= 0.5 ? 'bg-emerald-500/10 text-emerald-400' :
+            score >= 0.5 ? 'bg-sky-500/10 text-sky-400' :
             score >= 0.02 ? 'bg-yellow-500/10 text-yellow-400' :
             'bg-[var(--bg-card)] text-[var(--text-muted)]'
           )} title={`Search Score: ${score}`}>
@@ -290,15 +290,28 @@ function SourceItem({ source, index }) {
 
 export function DiagramLightbox({ images, currentIndex, onChangeIndex, onClose }) {
   const [zoom, setZoom] = useState(1)
+  const [pan, setPan] = useState({ x: 0, y: 0 })
+  const [isDragging, setIsDragging] = useState(false)
+  const [dragStart, setDragStart] = useState({ x: 0, y: 0 })
   const image = images[currentIndex]
 
   useEffect(() => {
     setZoom(1)
+    setPan({ x: 0, y: 0 })
   }, [currentIndex])
 
   const handleZoomIn = () => setZoom(z => Math.min(z + 0.25, 3))
-  const handleZoomOut = () => setZoom(z => Math.max(z - 0.25, 0.5))
-  const handleReset = () => setZoom(1)
+  const handleZoomOut = () => {
+    setZoom(z => {
+      const newZoom = Math.max(z - 0.25, 0.5)
+      if (newZoom <= 1) setPan({ x: 0, y: 0 })
+      return newZoom
+    })
+  }
+  const handleReset = () => {
+    setZoom(1)
+    setPan({ x: 0, y: 0 })
+  }
 
   const handlePrev = useCallback((e) => {
     if (e) e.stopPropagation()
@@ -316,10 +329,35 @@ export function DiagramLightbox({ images, currentIndex, onChangeIndex, onClose }
     const delta = e.deltaY;
     setZoom(z => {
       const newZoom = z - delta * zoomIntensity;
-      return Math.max(0.2, Math.min(newZoom, 5));
+      const finalZoom = Math.max(0.2, Math.min(newZoom, 5));
+      if (finalZoom <= 1) setPan({ x: 0, y: 0 })
+      return finalZoom;
     });
   }, []);
 
+  const handleMouseDown = (e) => {
+    if (zoom <= 1) return
+    e.preventDefault()
+    setIsDragging(true)
+    setDragStart({ x: e.clientX - pan.x, y: e.clientY - pan.y })
+  }
+
+  const handleMouseMove = (e) => {
+    if (!isDragging) return
+    setPan({
+      x: e.clientX - dragStart.x,
+      y: e.clientY - dragStart.y
+    })
+  }
+
+  const handleMouseUp = () => {
+    setIsDragging(false)
+  }
+
+  const handleMouseLeave = () => {
+    setIsDragging(false)
+  }
+
   useEffect(() => {
     const handleKeyDown = (e) => {
       if (e.key === 'Escape') onClose()
@@ -357,7 +395,15 @@ export function DiagramLightbox({ images, currentIndex, onChangeIndex, onClose }
           </div>
         </div>
         {/* Image container */}
-        <div className="diagram-lightbox-body relative" onWheel={handleWheel}>
+        <div 
+          className="diagram-lightbox-body relative" 
+          onWheel={handleWheel}
+          onMouseDown={handleMouseDown}
+          onMouseMove={handleMouseMove}
+          onMouseUp={handleMouseUp}
+          onMouseLeave={handleMouseLeave}
+          style={{ cursor: zoom > 1 ? (isDragging ? 'grabbing' : 'grab') : 'default' }}
+        >
           {images.length > 1 && (
             <button onClick={handlePrev} className="absolute left-4 p-2 bg-black/50 text-white rounded-full hover:bg-black/80 transition-colors z-10" title="Previous (Left Arrow)">
               <ChevronLeft size={24} />
@@ -366,8 +412,13 @@ export function DiagramLightbox({ images, currentIndex, onChangeIndex, onClose }
           <img
             src={image.url}
             alt={`Manual diagram — Page ${image.page}`}
-            style={{ transform: `scale(${zoom})`, transformOrigin: 'center center', transition: 'transform 0.05s ease-out' }}
+            style={{ 
+              transform: `translate(${pan.x}px, ${pan.y}px) scale(${zoom})`, 
+              transformOrigin: 'center center', 
+              transition: isDragging ? 'none' : 'transform 0.05s ease-out' 
+            }}
             className="diagram-lightbox-img"
+            draggable={false}
           />
           {images.length > 1 && (
             <button onClick={handleNext} className="absolute right-4 p-2 bg-black/50 text-white rounded-full hover:bg-black/80 transition-colors z-10" title="Next (Right Arrow)">
@@ -528,7 +579,7 @@ function AnswerBadge({ mode, validated }) {
   }
   if (validated) {
     return (
-      <span className="flex items-center gap-1 text-[11px] px-2 py-0.5 rounded-lg bg-emerald-500/10 text-emerald-400 border border-emerald-500/20">
+      <span className="flex items-center gap-1 text-[11px] px-2 py-0.5 rounded-lg bg-sky-500/10 text-sky-400 border border-sky-500/20">
         <CheckCircle size={10} /> Validated
       </span>
     )
@@ -571,7 +622,7 @@ function AssistantMessage({ response, streaming, streamStages, bookmarks, onTogg
     <div className="flex items-start gap-3 msg-enter">
       <div className="w-7 h-7 rounded-lg flex items-center justify-center shrink-0 mt-0.5"
         style={{ background: 'var(--bg-card)', border: '1px solid var(--border)' }}>
-        <Sparkles size={14} className="text-emerald-400" />
+        <Sparkles size={14} className="text-sky-400" />
       </div>
       <div className="flex-1 min-w-0 space-y-2 max-w-[85%]">
         {streaming && <StreamingIndicator />}
@@ -587,7 +638,7 @@ function AssistantMessage({ response, streaming, streamStages, bookmarks, onTogg
                           prose-ul:my-5 prose-ol:my-5
                           prose-li:my-3
                           prose-headings:font-semibold prose-headings:mt-7 prose-headings:mb-4
-                          prose-code:text-emerald-400 prose-code:px-1.5 prose-code:py-0.5 prose-code:rounded-md prose-code:text-[15px]
+                          prose-code:text-sky-400 prose-code:px-1.5 prose-code:py-0.5 prose-code:rounded-md prose-code:text-[15px]
                           prose-pre:rounded-xl prose-pre:my-5
                           prose-a:text-blue-400 prose-a:no-underline hover:prose-a:underline"
             style={{ '--tw-prose-body': 'var(--text-primary)', '--tw-prose-headings': 'var(--text-primary)', '--tw-prose-bold': 'var(--text-primary)', '--tw-prose-bullets': 'var(--text-muted)', '--tw-prose-counters': 'var(--text-muted)' }}>
diff --git a/frontend/src/components/Sidebar.jsx b/frontend/src/components/Sidebar.jsx
index 3c97bd044..3820ebf03 100644
--- a/frontend/src/components/Sidebar.jsx
+++ b/frontend/src/components/Sidebar.jsx
@@ -53,18 +53,18 @@ function ManualItem({ manual, isSelected, onToggle, onDelete }) {
       className={clsx(
         'group flex items-start gap-3 p-3 rounded-xl cursor-pointer transition-all duration-200',
         isSelected
-          ? 'bg-emerald-500/10 border border-emerald-500/20'
+          ? 'bg-transparent border border-[var(--border-hover)]'
           : 'hover:bg-[var(--bg-tertiary)] border border-transparent'
       )}
     >
       <div className={clsx('mt-0.5 shrink-0 w-4 h-4 rounded border flex items-center justify-center transition-colors',
-        isSelected ? 'bg-emerald-500 border-emerald-500' : 'border-[var(--border)]'
+        isSelected ? 'bg-[var(--text-secondary)] border-[var(--text-secondary)]' : 'border-[var(--border)]'
       )}>
         {isSelected && <div className="w-1.5 h-1.5 bg-white rounded-sm" />}
       </div>
       <div className="min-w-0 flex-1">
         <p className={clsx('text-sm font-medium truncate transition-colors',
-          isSelected ? 'text-emerald-300' : ''
+          isSelected ? 'text-[var(--text-primary)]' : ''
         )} style={isSelected ? {} : { color: 'var(--text-secondary)' }}>
           {manual.filename || manual.file_name}
         </p>
@@ -188,7 +188,7 @@ export default function Sidebar({ manuals, selectedIds, onToggle, onRefresh, loa
         <div className="flex px-4 pt-2 gap-4" style={{ borderBottom: '1px solid var(--border)' }}>
           <button
             onClick={() => setActiveTab('manuals')}
-            className={clsx('pb-2 text-sm font-medium transition-colors border-b-2', activeTab === 'manuals' ? 'border-emerald-500 text-emerald-400' : 'border-transparent text-[var(--text-muted)] hover:text-[var(--text-secondary)]')}
+            className={clsx('pb-2 text-sm font-medium transition-colors border-b-2', activeTab === 'manuals' ? 'border-sky-500 text-sky-500' : 'border-transparent text-[var(--text-muted)] hover:text-[var(--text-secondary)]')}
           >
             Manuals
           </button>
@@ -211,7 +211,7 @@ export default function Sidebar({ manuals, selectedIds, onToggle, onRefresh, loa
           </div>
           <div className="w-full h-1.5 bg-gray-500/20 rounded-full overflow-hidden">
             <div 
-              className="h-full bg-emerald-500 transition-all duration-300 ease-out" 
+              className="h-full bg-sky-500 transition-all duration-300 ease-out" 
               style={{ width: `${uploadProgress}%` }}
             />
           </div>
diff --git a/frontend/src/index.css b/frontend/src/index.css
index 6c4833fda..d57b9d788 100644
--- a/frontend/src/index.css
+++ b/frontend/src/index.css
@@ -1,43 +1,48 @@
-
 @tailwind base;
 @tailwind components;
 @tailwind utilities;
 
 @layer base {
   :root {
-    --bg-primary: #0a0a0a;
-    --bg-secondary: #141414;
-    --bg-tertiary: #1a1a1a;
-    --bg-card: #1e1e1e;
-    --bg-hover: #222222;
-    --border: #2a2a2a;
-    --border-hover: #333333;
-    --border-subtle: #222222;
-    --text-primary: #e8e8e8;
-    --text-secondary: #a0a0a0;
-    --text-muted: #666666;
-    --accent-green: #4ade80;
+    --bg-primary: #18181b;
+    --bg-secondary: #27272a;
+    --bg-tertiary: #3f3f46;
+    --bg-card: #27272a;
+    --bg-hover: #3f3f46;
+    --border: #52525b;
+    --border-hover: #71717a;
+    --border-subtle: #3f3f46;
+    --text-primary: #f4f4f5;
+    --text-secondary: #a1a1aa;
+    --text-muted: #71717a;
+    --accent-primary: #0092BC;
   }
 
   /* ── Light theme ──────────────────────────────────────── */
   .light {
-    --bg-primary: #f5f5f0;
+    --bg-primary: #f4f4f5;
     --bg-secondary: #ffffff;
-    --bg-tertiary: #f0f0ec;
+    --bg-tertiary: #e4e4e7;
     --bg-card: #ffffff;
-    --bg-hover: #f0f0ec;
-    --border: #e0e0dc;
-    --border-hover: #d0d0cc;
-    --border-subtle: #eaeae6;
-    --text-primary: #1a1a1a;
-    --text-secondary: #6b6b6b;
-    --text-muted: #999999;
-    --accent-green: #22c55e;
+    --bg-hover: #e4e4e7;
+    --border: #d4d4d8;
+    --border-hover: #a1a1aa;
+    --border-subtle: #e4e4e7;
+    --text-primary: #18181b;
+    --text-secondary: #52525b;
+    --text-muted: #71717a;
+    --accent-primary: #006F8F;
   }
 
-  * { box-sizing: border-box; margin: 0; padding: 0; }
+  * {
+    box-sizing: border-box;
+    margin: 0;
+    padding: 0;
+  }
 
-  html, body, #root {
+  html,
+  body,
+  #root {
     height: 100%;
     background: var(--bg-primary);
     color: var(--text-primary);
@@ -48,19 +53,41 @@
     -moz-osx-font-smoothing: grayscale;
   }
 
-  h1, h2, h3, h4, h5, h6 {
+  h1,
+  h2,
+  h3,
+  h4,
+  h5,
+  h6 {
     font-family: 'Inter', system-ui, sans-serif;
     letter-spacing: -0.02em;
     font-weight: 600;
   }
 
-  ::-webkit-scrollbar { width: 6px; }
-  ::-webkit-scrollbar-track { background: transparent; }
-  ::-webkit-scrollbar-thumb { background: rgba(255,255,255,0.08); border-radius: 10px; }
-  ::-webkit-scrollbar-thumb:hover { background: rgba(255,255,255,0.15); }
+  ::-webkit-scrollbar {
+    width: 6px;
+  }
+
+  ::-webkit-scrollbar-track {
+    background: transparent;
+  }
+
+  ::-webkit-scrollbar-thumb {
+    background: rgba(255, 255, 255, 0.08);
+    border-radius: 10px;
+  }
+
+  ::-webkit-scrollbar-thumb:hover {
+    background: rgba(255, 255, 255, 0.15);
+  }
+
+  .light ::-webkit-scrollbar-thumb {
+    background: rgba(0, 0, 0, 0.1);
+  }
 
-  .light ::-webkit-scrollbar-thumb { background: rgba(0,0,0,0.1); }
-  .light ::-webkit-scrollbar-thumb:hover { background: rgba(0,0,0,0.2); }
+  .light ::-webkit-scrollbar-thumb:hover {
+    background: rgba(0, 0, 0, 0.2);
+  }
 }
 
 @layer components {
@@ -76,29 +103,30 @@
     border-radius: 12px;
     transition: all 0.2s ease;
   }
+
   .glass-card:hover {
     background: var(--bg-hover);
     border-color: var(--border-hover);
   }
 
   .btn-primary {
-    @apply px-5 py-2.5 rounded-xl font-medium text-sm cursor-pointer
-           transition-all duration-200
-           disabled:opacity-40 disabled:cursor-not-allowed;
-    background: var(--accent-green);
-    color: #0a0a0a;
+    @apply px-5 py-2.5 rounded-xl font-medium text-sm cursor-pointer transition-all duration-200 disabled:opacity-40 disabled:cursor-not-allowed;
+    background: var(--accent-primary);
+    color: #ffffff;
   }
+
   .btn-primary:hover:not(:disabled) {
-    background: #22c55e;
+    background: #006F8F;
   }
 
   .btn-ghost {
     @apply px-3 py-2 rounded-xl text-sm cursor-pointer font-medium transition-all duration-200;
     color: var(--text-secondary);
   }
+
   .btn-ghost:hover {
     color: var(--text-primary);
-    background: rgba(255,255,255,0.06);
+    background: rgba(255, 255, 255, 0.06);
   }
 
   .input-base {
@@ -108,12 +136,14 @@
     color: var(--text-primary);
     font-size: 15px;
   }
+
   .input-base::placeholder {
     color: var(--text-muted);
   }
+
   .input-base:focus {
     border-color: #3a3a3a;
-    box-shadow: 0 0 0 3px rgba(255,255,255,0.03);
+    box-shadow: 0 0 0 3px rgba(255, 255, 255, 0.03);
   }
 
   .card {
@@ -122,33 +152,39 @@
 
   .tag {
     @apply inline-flex items-center gap-1.5 px-2.5 py-1 rounded-lg text-xs font-medium;
-    background: rgba(255,255,255,0.06);
+    background: rgba(255, 255, 255, 0.06);
     color: var(--text-secondary);
     border: 1px solid var(--border-subtle);
   }
 }
 
-/* ── Green gradient orb ───────────────────────────────────────────────── */
-.green-orb {
+/* ── Brand gradient orb ───────────────────────────────────────────────── */
+.brand-orb {
   width: 80px;
   height: 80px;
   border-radius: 50%;
-  background: radial-gradient(circle at 30% 30%, #86efac, #22c55e 50%, #166534);
+  background: radial-gradient(circle at 30% 30%, #7AC6DC, #0092BC 50%, #006F8F);
   box-shadow:
-    0 0 60px rgba(34, 197, 94, 0.3),
-    0 0 120px rgba(34, 197, 94, 0.1);
-  animation: orb-float 6s ease-in-out infinite;
+    0 0 60px rgba(0, 146, 188, 0.3),
+    0 0 120px rgba(0, 146, 188, 0.1);
 }
 
-.light .green-orb {
+.light .brand-orb {
   box-shadow:
-    0 0 50px rgba(34, 197, 94, 0.25),
-    0 0 100px rgba(34, 197, 94, 0.08);
+    0 0 50px rgba(0, 111, 143, 0.25),
+    0 0 100px rgba(0, 111, 143, 0.08);
 }
 
 @keyframes orb-float {
-  0%, 100% { transform: translateY(0px) rotate(0deg); }
-  50% { transform: translateY(-8px) rotate(5deg); }
+
+  0%,
+  100% {
+    transform: translateY(0px) rotate(0deg);
+  }
+
+  50% {
+    transform: translateY(-8px) rotate(5deg);
+  }
 }
 
 /* ── Suggestion cards ─────────────────────────────────────────────────── */
@@ -167,13 +203,15 @@
   text-align: left;
   box-shadow: 0 1px 2px rgba(0, 0, 0, 0.05);
 }
+
 .suggestion-card:hover {
   background: var(--bg-hover);
-  border-color: var(--accent-green);
+  border-color: var(--accent-primary);
   color: var(--text-primary);
   transform: translateY(-2px);
   box-shadow: 0 6px 12px rgba(0, 0, 0, 0.08);
 }
+
 .suggestion-card:disabled {
   opacity: 0.4;
   cursor: not-allowed;
@@ -187,14 +225,22 @@
   background: var(--text-muted);
   display: inline-block;
   margin: 0 2px;
-  animation: dot-pulse 1.4s ease-in-out infinite;
+  margin: 0 2px;
 }
-.typing-dot:nth-child(2) { animation-delay: 0.2s; }
-.typing-dot:nth-child(3) { animation-delay: 0.4s; }
 
 @keyframes dot-pulse {
-  0%, 80%, 100% { opacity: 0.3; transform: scale(0.8); }
-  40% { opacity: 1; transform: scale(1); }
+
+  0%,
+  80%,
+  100% {
+    opacity: 0.3;
+    transform: scale(0.8);
+  }
+
+  40% {
+    opacity: 1;
+    transform: scale(1);
+  }
 }
 
 .msg-enter {
@@ -206,22 +252,29 @@
   width: 6px;
   height: 6px;
   border-radius: 50%;
-  background: var(--accent-green);
+  background: var(--accent-primary);
   display: inline-block;
   margin: 0 2px;
-  animation: dot-bounce 0.6s infinite alternate;
+  animation: dot-bounce 0.6s infinite alternate ease-in-out;
+}
+.bouncy-dot:nth-child(2) {
+  animation-delay: 0.2s;
+}
+.bouncy-dot:nth-child(3) {
+  animation-delay: 0.4s;
 }
-.bouncy-dot:nth-child(2) { animation-delay: 0.2s; }
-.bouncy-dot:nth-child(3) { animation-delay: 0.4s; }
 
 @keyframes dot-bounce {
-  to { transform: translateY(-6px); opacity: 0.5; }
+  to {
+    transform: translateY(-6px);
+    opacity: 0.5;
+  }
 }
 
 
 .progress-bar {
   height: 2px;
-  background: var(--accent-green);
+  background: var(--accent-primary);
   border-radius: 2px;
 }
 
@@ -240,14 +293,17 @@
   box-shadow: 0 25px 60px rgba(0, 0, 0, 0.5);
 }
 
-.ki-manual-card, .ki-model-card {
+.ki-manual-card,
+.ki-model-card {
   @apply p-4;
   background: var(--bg-tertiary);
   border: 1px solid var(--border);
   border-radius: 12px;
   transition: background 0.2s;
 }
-.ki-manual-card:hover, .ki-model-card:hover {
+
+.ki-manual-card:hover,
+.ki-model-card:hover {
   background: var(--bg-hover);
 }
 
@@ -265,6 +321,7 @@
   border-radius: 12px;
   transition: background 0.2s;
 }
+
 .ki-stat-card:hover {
   background: var(--bg-hover);
 }
@@ -287,9 +344,9 @@
   cursor: pointer;
   transition: all 0.25s ease;
   background: var(--bg-secondary);
-  animation: diagram-fade-in 0.3s ease forwards;
-  opacity: 0;
+  background: var(--bg-secondary);
 }
+
 .diagram-thumbnail-card:hover {
   border-color: var(--border-hover);
   box-shadow: 0 4px 20px rgba(0, 0, 0, 0.25);
@@ -297,7 +354,9 @@
 }
 
 @keyframes diagram-fade-in {
-  to { opacity: 1; }
+  to {
+    opacity: 1;
+  }
 }
 
 .diagram-thumbnail-wrapper {
@@ -314,6 +373,7 @@
   object-position: top center;
   transition: transform 0.3s ease;
 }
+
 .diagram-thumbnail-card:hover .diagram-thumbnail-img {
   transform: scale(1.04);
 }
@@ -328,6 +388,7 @@
   opacity: 0;
   transition: opacity 0.25s ease;
 }
+
 .diagram-thumbnail-card:hover .diagram-thumbnail-overlay {
   opacity: 1;
 }
@@ -351,12 +412,17 @@
   padding: 20px;
   background: rgba(0, 0, 0, 0.82);
   backdrop-filter: blur(12px);
-  animation: lightbox-overlay-in 0.2s ease;
+  backdrop-filter: blur(12px);
 }
 
 @keyframes lightbox-overlay-in {
-  from { opacity: 0; }
-  to { opacity: 1; }
+  from {
+    opacity: 0;
+  }
+
+  to {
+    opacity: 1;
+  }
 }
 
 .diagram-lightbox-content {
@@ -370,12 +436,19 @@
   border-radius: 16px;
   box-shadow: 0 30px 80px rgba(0, 0, 0, 0.6);
   overflow: hidden;
-  animation: lightbox-content-in 0.25s ease;
+  overflow: hidden;
 }
 
 @keyframes lightbox-content-in {
-  from { opacity: 0; transform: scale(0.95); }
-  to { opacity: 1; transform: scale(1); }
+  from {
+    opacity: 0;
+    transform: scale(0.95);
+  }
+
+  to {
+    opacity: 1;
+    transform: scale(1);
+  }
 }
 
 .diagram-lightbox-header {
@@ -400,10 +473,12 @@
   cursor: pointer;
   transition: all 0.2s ease;
 }
+
 .diagram-lightbox-btn:hover {
   background: var(--bg-hover);
   color: var(--text-primary);
 }
+
 .diagram-lightbox-close:hover {
   background: rgba(239, 68, 68, 0.15);
   color: #ef4444;
@@ -432,9 +507,11 @@
 .light .diagram-thumbnail-card:hover {
   box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
 }
+
 .light .diagram-lightbox-overlay {
   background: rgba(0, 0, 0, 0.6);
 }
+
 .light .diagram-lightbox-img {
   box-shadow: 0 4px 30px rgba(0, 0, 0, 0.15);
 }
@@ -446,12 +523,15 @@
   border-collapse: collapse;
   margin: 1.25rem 0;
 }
-.prose th, .prose td {
+
+.prose th,
+.prose td {
   border: 1px solid var(--border);
   padding: 0.75rem;
   text-align: left;
 }
+
 .prose th {
   background: var(--bg-tertiary);
   font-weight: 600;
-}
+}
\ No newline at end of file
